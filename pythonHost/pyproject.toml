[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pythonHost"
version = "0.1.0"
dependencies = [
  "pyserial",
  "zerorpc",
  "fire",
]

[tool.setuptools]
package-dir = {"" = ""}

[tool.setuptools.packages.find]
where = [""]
